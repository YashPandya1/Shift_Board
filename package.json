{
  "name": "shiftboard",
  "version": "1.0.0",
  "private": true,
  "description": "ShiftBoard - Employee Scheduling Platform",
  "scripts": {
    "dev": "concurrently -k -n api,web -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm start --prefix backend",
    "build": "npm run build --prefix frontend",
    "install:all": "npm install --prefix backend && npm install --prefix frontend --legacy-peer-deps",
    "seed": "npm run seed --prefix backend",
    "db:clear": "npm run db:clear --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
